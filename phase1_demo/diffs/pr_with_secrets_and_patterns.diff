diff --git a/app.py b/app.py
index 1111111..2222222 100644
--- a/app.py
+++ b/app.py
@@ -1,2 +1,8 @@
+import hashlib
 from flask import Flask, request
 app = Flask(__name__)
+
+api_key = "ghp_ABCDEFghijklmnopqrstuvwxyz0123456789"
+query = "SELECT * FROM users WHERE id = " + request.args.get("id")
+html = "<div>" + request.args.get("q") + "</div>"
+element.innerHTML = html
+hash_val = hashlib.md5(request.args.get("password", "").encode()).hexdigest()
